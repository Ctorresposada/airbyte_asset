version: 2

sources:
  - name: connect20_bronze
    description: "Connect20 / ESCworks evaluation data — bronze Iceberg tables in escr20_bronze_dev."
    database: awsdatacatalog
    schema: escr20_bronze_dev
    tables:
      - name: connect20_evaluations
        description: "ESCworks evaluation information records ingested from raw Parquet via dbt bronze."
      - name: connect20_schedule12
        description: "ESCworks session schedule records ingested from raw Parquet via dbt bronze."
      - name: connect20_sessioninformation
        description: "ESCworks session attendance and registration records ingested from raw Parquet via dbt bronze."
