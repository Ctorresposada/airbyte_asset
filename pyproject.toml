[project]
name = "nd_predictive_ordering"
version = "0.1.0"
description = "A project to manage the end to end flow for predictive ordering"
readme = "README.md"

requires-python = ">=3.12"
dependencies = [
    "boto3",
    ]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
]
