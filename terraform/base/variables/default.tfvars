environment  = "shared"    # Deployment environment name, ex "shared"
aws_region   = "us-east-1" # Target AWS region for all base resources, ex "us-east-1"
team         = "devops"    # Owning team tag applied to every resource, ex "devops"
company_name = "region-20" # Resource name prefix used across all stacks, ex "region-20"
